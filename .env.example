# PSAW (Pushshift) doesn't need API keys for historical data.
# Just use it directly in your code.

# Reddit API credentials (if using direct Reddit authentication)
# Get these from https://www.reddit.com/prefs/apps
REDDIT_CLIENT_ID=your_client_id_here
REDDIT_CLIENT_SECRET=your_client_secret_here
REDDIT_USER_AGENT=animus-mercatus/1.0 by your_username

# Note: PSAW (Pushshift) doesn't need API keys for historical data
# You can use it directly without credentials